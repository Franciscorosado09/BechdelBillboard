<h1>Delicious Hamburgers</h1>
<div class="row">
  <div class="col-4">
    <h2>Pick a hamburger to eat.</h2>

    <ul>
      {{#each burger}}
      {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
      {{/unless}}
      {{/each}}
    </ul>
  </div>

  <div class="col-4">
    <h2>Burgers that have been eaten.</h2>

    <ul>
      {{#each burger}}
      {{#if devoured}}
      {{> burgers/burger2-block}}
      {{/if}}
      {{/each}}
    </ul>
  </div>
</div>
<h2>Add a Burger</h2>

<form id="create-form">

  <div class="form-group">
    <label for="burger">Burger Name:</label>
    <input type="text" id="burger" name="burger_name">
  </div>
  </div>


  {{!-- <div class="form-group">
    <label for="devoured">Eaten?</label><br>
    <input type="radio" name="devoured" value="1" id="devoured" checked> Devoured!<br>
    <input type="radio" name="devoured" value="0" id="notEaten"> Not eaten
  </div> --}}

  <button type="submit">Add Burger</button>
</form>